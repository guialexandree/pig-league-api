

<tests>
    <IMPORTANTE>nos testes NUNCA use dados truncados, sempre use o faker</IMPORTANTE>
</tests>

<imports>
    <IMPORTANTE>use o alias `@` para caminhos a partir de `src` (ex.: `@/infra/...`) e evite imports relativos com `../../`</IMPORTANTE>
</imports>
